def postman_paste():
    # with open(FILES.joinpath("headers.json"), "w") as file:
    #     json.dump(headers, file)
    # with open(FILES.joinpath("url.json"), "w") as file2:
    #     json.dump(url, file2)
    pass


if __name__ == "__main__":
    postman_paste()
